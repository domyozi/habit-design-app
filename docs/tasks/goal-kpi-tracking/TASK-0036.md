# TASK-0036: MonthlyTab KGI/KPI 設定 UI

**タスクID**: TASK-0036
**タスクタイプ**: TDD
**推定工数**: 8h
**フェーズ**: Phase 3 - フロントエンド実装
**信頼性レベル**: 🔵 *dataflow.md #1 #2 より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/goal-kpi-tracking/requirements.md) - REQ-KGI-001〜003, REQ-KPI-001〜007, EDGE-KPI-001〜003
- **設計文書**: [📐 architecture.md](../../design/goal-kpi-tracking/architecture.md)
- **型定義**: [📝 interfaces.ts](../../design/goal-kpi-tracking/interfaces.ts)
- **API仕様**: [🔌 api-endpoints.md](../../design/goal-kpi-tracking/api-endpoints.md)

## タスク概要

MonthlyTab（月次画面）に KGI/KPI 設定フローを追加する。既存の Goal カードに「KGI化」ボタンを追加し、KGI 設定フォームモーダル（`KgiSetupForm`）・KPI 追加フォーム（`KpiCreateForm`）・習慣連結モーダル（`HabitLinkModal`）の3コンポーネントを実装する。KGI 展開時に KPI 一覧を表示し、各 KPI から習慣連結・削除ができるようにする。

## 依存タスク

- **前提タスク**: [TASK-0034: frontend-v2 型定義拡張 + APIクライアント追加](TASK-0034.md), [TASK-0035: KgiCard コンポーネント実装](TASK-0035.md)
- **後続タスク**: [TASK-0039: E2E テスト](TASK-0039.md)

## 完了条件

- [ ] Goal カードから KGI 設定フォームが開き、保存できる
- [ ] KGI カードから KPI 追加フォームが開き、保存できる
- [ ] KPI から習慣連結モーダルが開き、保存できる
- [ ] KPI 削除が確認ダイアログ付きで動作する
- [ ] TDD で全テストケースが通過している

---

## 実装詳細

### 1. Goal カードへの「KGI化」ボタン追加 🔵

**信頼性**: 🔵 *REQ-KGI-001 より*

既存の Goal 表示コンポーネントに最小限の変更で「KGI化」ボタンを追加する。`is_kgi=false` の Goal にのみ表示する。

**実装ファイル**: MonthlyTab 内の既存 Goal 表示コンポーネント（`frontend-v2/src/pages/` 内を確認）

```typescript
// 既存の Goal カードコンポーネントへの追加イメージ
// KGI化ボタンは KgiCard コンポーネントの外側（MonthlyTab レベル）で管理する

// MonthlyTab.tsx（既存コンポーネントへの追加）
{!goal.is_kgi && (
  <button
    className="goal-card__kgi-button"
    onClick={() => openKgiSetupForm(goal.id)}
  >
    KGI化
  </button>
)}
```

### 2. KgiSetupForm コンポーネント 🔵

**信頼性**: 🔵 *REQ-KGI-002〜003 より*

Goal を KGI として設定するためのフォームモーダル。`target_date` は必須、metric_type によって target_value フィールドの表示を切り替える。

**実装ファイル**: `frontend-v2/src/components/KgiSetupForm.tsx`

```typescript
import React, { useState } from 'react';
import { SetKgiRequest, MetricType } from '../types';
import { patchGoalKgi } from '../lib/api';

export interface KgiSetupFormProps {
  goalId: string;
  goalTitle: string;
  onSuccess: () => void;
  onClose: () => void;
}

interface FormState {
  metric_type: MetricType;
  target_value: string;
  unit: string;
  target_date: string;
  current_value: string;
}

interface FormErrors {
  target_date?: string;
  target_value?: string;
}

export function KgiSetupForm({ goalId, goalTitle, onSuccess, onClose }: KgiSetupFormProps) {
  const [form, setForm] = useState<FormState>({
    metric_type: 'numeric',
    target_value: '',
    unit: '',
    target_date: '',
    current_value: '',
  });
  const [errors, setErrors] = useState<FormErrors>({});
  const [isSubmitting, setIsSubmitting] = useState(false);

  const validate = (): boolean => {
    const newErrors: FormErrors = {};

    if (!form.target_date) {
      newErrors.target_date = 'TARGET_DATE_REQUIRED';
    }
    if (form.metric_type === 'percentage' && form.target_value !== '') {
      const val = parseFloat(form.target_value);
      if (val < 0 || val > 100) {
        newErrors.target_value = 'PERCENTAGE_OUT_OF_RANGE';
      }
    }

    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    if (!validate()) return;

    setIsSubmitting(true);
    try {
      const req: SetKgiRequest = {
        metric_type: form.metric_type,
        target_date: form.target_date,
        target_value: form.target_value !== '' ? parseFloat(form.target_value) : undefined,
        unit: form.unit || undefined,
        current_value: form.current_value !== '' ? parseFloat(form.current_value) : undefined,
      };
      await patchGoalKgi(goalId, req);
      onSuccess();
    } catch {
      // エラーハンドリング（API エラー）
    } finally {
      setIsSubmitting(false);
    }
  };

  const isBinary = form.metric_type === 'binary';

  return (
    <div role="dialog" aria-modal="true" aria-label="KGI設定">
      <h2>{goalTitle} を KGI として設定</h2>
      <form onSubmit={handleSubmit}>
        {/* 指標タイプ */}
        <label htmlFor="metric-type">指標タイプ</label>
        <select
          id="metric-type"
          value={form.metric_type}
          onChange={(e) => setForm({ ...form, metric_type: e.target.value as MetricType })}
        >
          <option value="numeric">数値</option>
          <option value="percentage">パーセンテージ</option>
          <option value="binary">達成/未達成</option>
        </select>

        {/* 目標値（binary 以外） */}
        {!isBinary && (
          <>
            <label htmlFor="target-value">目標値</label>
            <input
              id="target-value"
              type="number"
              value={form.target_value}
              min={form.metric_type === 'percentage' ? 0 : undefined}
              max={form.metric_type === 'percentage' ? 100 : undefined}
              onChange={(e) => setForm({ ...form, target_value: e.target.value })}
            />
            {errors.target_value === 'PERCENTAGE_OUT_OF_RANGE' && (
              <span role="alert">目標値は 0〜100 の範囲で入力してください</span>
            )}
          </>
        )}

        {/* 単位（最大20字、binary 以外） */}
        {!isBinary && (
          <>
            <label htmlFor="unit">単位</label>
            <input
              id="unit"
              type="text"
              value={form.unit}
              maxLength={20}
              onChange={(e) => setForm({ ...form, unit: e.target.value })}
              placeholder="例: km, 冊, 件"
            />
          </>
        )}

        {/* 目標日（必須） */}
        <label htmlFor="target-date">目標日 *</label>
        <input
          id="target-date"
          type="date"
          value={form.target_date}
          onChange={(e) => setForm({ ...form, target_date: e.target.value })}
          required
        />
        {errors.target_date === 'TARGET_DATE_REQUIRED' && (
          <span role="alert">目標日は必須です</span>
        )}

        {/* 現在値（任意） */}
        {!isBinary && (
          <>
            <label htmlFor="current-value">現在値（任意）</label>
            <input
              id="current-value"
              type="number"
              value={form.current_value}
              onChange={(e) => setForm({ ...form, current_value: e.target.value })}
            />
          </>
        )}

        <button
          type="submit"
          disabled={!form.target_date || isSubmitting}
        >
          {isSubmitting ? '保存中...' : '保存'}
        </button>
        <button type="button" onClick={onClose}>キャンセル</button>
      </form>
    </div>
  );
}
```

### 3. KpiCreateForm コンポーネント 🔵

**信頼性**: 🔵 *REQ-KPI-001〜003 より*

KGI に紐づく KPI を作成するフォーム。`title` は必須。`percentage` 型では `target_value` を 0〜100 に制限する。

**実装ファイル**: `frontend-v2/src/components/KpiCreateForm.tsx`

```typescript
import React, { useState } from 'react';
import { CreateKpiRequest, MetricType, TrackingFrequency } from '../types';
import { createKpi } from '../lib/api';

export interface KpiCreateFormProps {
  goalId: string;
  onSuccess: () => void;
  onClose: () => void;
}

export function KpiCreateForm({ goalId, onSuccess, onClose }: KpiCreateFormProps) {
  const [form, setForm] = useState({
    title: '',
    metric_type: 'numeric' as MetricType,
    target_value: '',
    unit: '',
    tracking_frequency: 'daily' as TrackingFrequency,
  });
  const [isSubmitting, setIsSubmitting] = useState(false);
  const [errors, setErrors] = useState<{ target_value?: string }>({});

  const isBinary = form.metric_type === 'binary';

  const validate = (): boolean => {
    const newErrors: { target_value?: string } = {};
    if (form.metric_type === 'percentage' && form.target_value !== '') {
      const val = parseFloat(form.target_value);
      if (val < 0 || val > 100) {
        newErrors.target_value = 'PERCENTAGE_OUT_OF_RANGE';
      }
    }
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    if (!form.title.trim() || !validate()) return;

    setIsSubmitting(true);
    try {
      const req: CreateKpiRequest = {
        goal_id: goalId,
        title: form.title.trim(),
        metric_type: form.metric_type,
        target_value: !isBinary && form.target_value !== ''
          ? parseFloat(form.target_value)
          : undefined,
        unit: form.unit || undefined,
        tracking_frequency: form.tracking_frequency,
      };
      await createKpi(req);
      onSuccess();
    } catch {
      // エラーハンドリング
    } finally {
      setIsSubmitting(false);
    }
  };

  return (
    <div role="dialog" aria-modal="true" aria-label="KPI追加">
      <h2>KPI を追加</h2>
      <form onSubmit={handleSubmit}>
        {/* タイトル（必須） */}
        <label htmlFor="kpi-title">タイトル *</label>
        <input
          id="kpi-title"
          type="text"
          value={form.title}
          onChange={(e) => setForm({ ...form, title: e.target.value })}
          required
        />

        {/* 指標タイプ */}
        <label htmlFor="kpi-metric-type">指標タイプ</label>
        <select
          id="kpi-metric-type"
          value={form.metric_type}
          onChange={(e) => setForm({ ...form, metric_type: e.target.value as MetricType })}
        >
          <option value="numeric">数値</option>
          <option value="percentage">パーセンテージ</option>
          <option value="binary">達成/未達成</option>
        </select>

        {/* 目標値（binary 以外） */}
        {!isBinary && (
          <>
            <label htmlFor="kpi-target-value">目標値</label>
            <input
              id="kpi-target-value"
              type="number"
              value={form.target_value}
              min={form.metric_type === 'percentage' ? 0 : undefined}
              max={form.metric_type === 'percentage' ? 100 : undefined}
              onChange={(e) => setForm({ ...form, target_value: e.target.value })}
            />
            {errors.target_value === 'PERCENTAGE_OUT_OF_RANGE' && (
              <span role="alert">目標値は 0〜100 の範囲で入力してください</span>
            )}
          </>
        )}

        {/* 単位（binary 以外） */}
        {!isBinary && (
          <>
            <label htmlFor="kpi-unit">単位</label>
            <input
              id="kpi-unit"
              type="text"
              value={form.unit}
              onChange={(e) => setForm({ ...form, unit: e.target.value })}
            />
          </>
        )}

        {/* 追跡頻度 */}
        <label htmlFor="kpi-frequency">追跡頻度</label>
        <select
          id="kpi-frequency"
          value={form.tracking_frequency}
          onChange={(e) => setForm({ ...form, tracking_frequency: e.target.value as TrackingFrequency })}
        >
          <option value="daily">毎日</option>
          <option value="weekly">毎週</option>
          <option value="monthly">毎月</option>
        </select>

        <button type="submit" disabled={!form.title.trim() || isSubmitting}>
          {isSubmitting ? '追加中...' : '追加'}
        </button>
        <button type="button" onClick={onClose}>キャンセル</button>
      </form>
    </div>
  );
}
```

### 4. HabitLinkModal コンポーネント 🔵

**信頼性**: 🔵 *REQ-KPI-006 より*

既存習慣一覧をチェックボックスで表示し、KPI に紐づける習慣を選択できるモーダル。選択後に `linkKpiHabits` で全上書き保存する。

**実装ファイル**: `frontend-v2/src/components/HabitLinkModal.tsx`

```typescript
import React, { useState } from 'react';
import { linkKpiHabits } from '../lib/api';

export interface Habit {
  id: string;
  title: string;
}

export interface HabitLinkModalProps {
  kpiId: string;
  kpiTitle: string;
  availableHabits: Habit[];
  initialSelectedIds?: string[];
  onSuccess: (selectedIds: string[]) => void;
  onClose: () => void;
}

export function HabitLinkModal({
  kpiId,
  kpiTitle,
  availableHabits,
  initialSelectedIds = [],
  onSuccess,
  onClose,
}: HabitLinkModalProps) {
  const [selectedIds, setSelectedIds] = useState<Set<string>>(
    new Set(initialSelectedIds)
  );
  const [isSubmitting, setIsSubmitting] = useState(false);

  const toggleHabit = (habitId: string) => {
    setSelectedIds((prev) => {
      const next = new Set(prev);
      if (next.has(habitId)) {
        next.delete(habitId);
      } else {
        next.add(habitId);
      }
      return next;
    });
  };

  const handleSave = async () => {
    setIsSubmitting(true);
    try {
      const ids = Array.from(selectedIds);
      await linkKpiHabits(kpiId, ids);
      onSuccess(ids);
    } catch {
      // エラーハンドリング
    } finally {
      setIsSubmitting(false);
    }
  };

  return (
    <div role="dialog" aria-modal="true" aria-label="関連習慣を選択">
      <h2>{kpiTitle} に関連する習慣を選択</h2>
      <ul>
        {availableHabits.map((habit) => (
          <li key={habit.id}>
            <label>
              <input
                type="checkbox"
                checked={selectedIds.has(habit.id)}
                onChange={() => toggleHabit(habit.id)}
              />
              {habit.title}
            </label>
          </li>
        ))}
      </ul>
      <button onClick={handleSave} disabled={isSubmitting}>
        {isSubmitting ? '保存中...' : '保存'}
      </button>
      <button onClick={onClose}>キャンセル</button>
    </div>
  );
}
```

### 5. KPI 一覧表示（MonthlyTab 内 KGI 展開時） 🟡

**信頼性**: 🟡 *UI 設計は確定待ち*

KGI として設定された Goal を展開すると、紐づく KPI 一覧が表示される。各 KPI カードには「関連習慣を選択」・「削除」ボタンがある。

**実装ファイル**: `frontend-v2/src/pages/MonthlyTab.tsx`（既存ファイルに追加）

```typescript
// KPI 一覧表示の実装イメージ（MonthlyTab 内）

function KpiList({ goalId }: { goalId: string }) {
  const [kpis, setKpis] = useState<Kpi[]>([]);
  const [habitModalKpiId, setHabitModalKpiId] = useState<string | null>(null);

  useEffect(() => {
    fetchKpis(goalId).then((res) => {
      if (res.success) setKpis(res.data);
    });
  }, [goalId]);

  const handleDelete = async (kpiId: string) => {
    if (!window.confirm('この KPI を削除しますか？')) return;
    await deleteKpi(kpiId);
    setKpis((prev) => prev.filter((k) => k.id !== kpiId));
  };

  return (
    <ul className="kpi-list">
      {kpis.map((kpi) => (
        <li key={kpi.id} className="kpi-list__item">
          <span className="kpi-list__title">{kpi.title}</span>
          <span className="kpi-list__type">{kpi.metric_type}</span>
          {kpi.target_value !== undefined && (
            <span className="kpi-list__target">
              {kpi.target_value}{kpi.unit ? ` ${kpi.unit}` : ''}
            </span>
          )}
          <span className="kpi-list__frequency">{kpi.tracking_frequency}</span>
          {kpi.habit_ids && kpi.habit_ids.length > 0 && (
            <span className="kpi-list__habits">
              → {/* 習慣名は別途取得またはキャッシュから参照 */}
            </span>
          )}
          <button onClick={() => setHabitModalKpiId(kpi.id)}>
            関連習慣を選択
          </button>
          <button onClick={() => handleDelete(kpi.id)}>削除</button>
        </li>
      ))}
      {habitModalKpiId && (
        <HabitLinkModal
          kpiId={habitModalKpiId}
          kpiTitle={kpis.find((k) => k.id === habitModalKpiId)?.title ?? ''}
          availableHabits={[]} // 親コンポーネントから渡す
          onSuccess={() => setHabitModalKpiId(null)}
          onClose={() => setHabitModalKpiId(null)}
        />
      )}
    </ul>
  );
}
```

---

## 単体テスト要件

### テストケース1: 通常 Goal カードに「KGI化」ボタンが表示される 🔵

**Given**: `goal.is_kgi = false` の Goal が MonthlyTab に表示されている
**When**: MonthlyTab をレンダリングする
**Then**: 「KGI化」ボタンが Goal カード内に存在する

### テストケース2: KGI 設定フォームで target_date 未入力時、送信ボタンが disabled 🔵

**Given**: `KgiSetupForm` がレンダリングされており、`target_date` フィールドが空の状態
**When**: フォームの状態を確認する
**Then**: 「保存」ボタンが `disabled` 属性を持っている（REQ-KGI-002）

### テストケース3: percentage 型で target_value が 101 以上のときエラー表示 🔵

**Given**: `KpiCreateForm` で `metric_type = 'percentage'` を選択し、`target_value = 101` を入力した状態
**When**: フォームを送信する
**Then**: 「目標値は 0〜100 の範囲で入力してください」エラーメッセージが表示される（EDGE-KPI-002）

### テストケース4: 「KGI化」成功後、GoalカードがKgiCard表示に切り替わる 🔵

**Given**: `patchGoalKgi` がモックされており成功を返す状態
**When**: KGI 設定フォームに正しい値を入力して送信する
**Then**: `onSuccess` コールバックが呼ばれ、Goal カードが KgiCard 表示に切り替わる

### テストケース5: 習慣連結後、KPIカードに連結習慣名が表示される 🟡

**Given**: `linkKpiHabits` がモックされており成功を返す状態
**When**: HabitLinkModal で習慣を選択して保存する
**Then**: KPI カードに選択した習慣名が「→ {習慣名}」形式で表示される（REQ-KPI-006）

### テストケース6: KPI 削除確認ダイアログ表示後、「確認」で KPI が一覧から消える 🟡

**Given**: KPI 一覧が表示されており、`deleteKpi` がモックされている
**When**: 「削除」ボタンをクリックし、確認ダイアログで「OK」を選択する
**Then**: 削除した KPI が一覧から消える（`deleteKpi` が1回呼ばれる）

---

テストファイル例（主要テストケース）:

```typescript
// frontend-v2/src/components/KgiSetupForm.test.tsx

import { render, screen } from '@testing-library/react';
import { KgiSetupForm } from './KgiSetupForm';

describe('KgiSetupForm', () => {
  const baseProps = {
    goalId: 'goal-1',
    goalTitle: 'テスト目標',
    onSuccess: vi.fn(),
    onClose: vi.fn(),
  };

  // TC2: target_date 未入力時に保存ボタンが disabled
  test('target_date が空のとき保存ボタンが disabled', () => {
    render(<KgiSetupForm {...baseProps} />);
    const submitButton = screen.getByText('保存');
    expect(submitButton).toBeDisabled();
  });
});

// frontend-v2/src/components/KpiCreateForm.test.tsx

import { render, screen, fireEvent } from '@testing-library/react';
import { KpiCreateForm } from './KpiCreateForm';

describe('KpiCreateForm', () => {
  const baseProps = {
    goalId: 'goal-1',
    onSuccess: vi.fn(),
    onClose: vi.fn(),
  };

  // TC3: percentage 型で 101 以上のとき validation エラー
  test('percentage 型で target_value=101 のときエラー表示', async () => {
    render(<KpiCreateForm {...baseProps} />);

    // metric_type を percentage に変更
    fireEvent.change(screen.getByLabelText('指標タイプ'), {
      target: { value: 'percentage' },
    });

    // title 入力（submit 可能にするため）
    fireEvent.change(screen.getByLabelText('タイトル *'), {
      target: { value: 'テスト KPI' },
    });

    // target_value に 101 を入力
    fireEvent.change(screen.getByLabelText('目標値'), {
      target: { value: '101' },
    });

    // フォーム送信
    fireEvent.click(screen.getByText('追加'));

    // エラーメッセージ表示確認
    expect(
      screen.getByText('目標値は 0〜100 の範囲で入力してください')
    ).toBeInTheDocument();
  });
});

// frontend-v2/src/components/HabitLinkModal.test.tsx

import { render, screen, fireEvent, waitFor } from '@testing-library/react';
import { HabitLinkModal } from './HabitLinkModal';
import * as api from '../lib/api';

describe('HabitLinkModal', () => {
  const habits = [
    { id: 'habit-1', title: '朝のランニング' },
    { id: 'habit-2', title: '読書30分' },
  ];

  const baseProps = {
    kpiId: 'kpi-1',
    kpiTitle: 'テスト KPI',
    availableHabits: habits,
    onSuccess: vi.fn(),
    onClose: vi.fn(),
  };

  // TC5: 習慣選択後保存でコールバックが呼ばれる
  test('習慣を選択して保存すると onSuccess が呼ばれる', async () => {
    vi.spyOn(api, 'linkKpiHabits').mockResolvedValue({
      success: true,
      data: { kpi_id: 'kpi-1', habit_ids: ['habit-1'] },
    });

    render(<HabitLinkModal {...baseProps} />);

    // 「朝のランニング」を選択
    fireEvent.click(screen.getByLabelText('朝のランニング'));

    // 保存
    fireEvent.click(screen.getByText('保存'));

    await waitFor(() => {
      expect(baseProps.onSuccess).toHaveBeenCalledWith(['habit-1']);
    });
  });
});
```

---

## 実装手順（TDD）

1. **要件整理** `/tsumiki:tdd-requirements TASK-0036`
   - MonthlyTab での KGI/KPI 設定フローを図解する
   - 各コンポーネントの責務と状態管理を整理する
   - 既存の MonthlyTab 実装ファイルパスを `frontend-v2/src/pages/` 内で確認する

2. **テストケース洗い出し** `/tsumiki:tdd-testcases`
   - 上記「単体テスト要件」の6ケースをテストファイルに記述する
   - コンポーネントごとにファイルを分ける（KgiSetupForm.test.tsx, KpiCreateForm.test.tsx, HabitLinkModal.test.tsx）

3. **Red フェーズ** `/tsumiki:tdd-red`
   - テストを実行し、全件失敗（RED）することを確認する

4. **Green フェーズ** `/tsumiki:tdd-green`
   - `KgiSetupForm.tsx` → `KpiCreateForm.tsx` → `HabitLinkModal.tsx` の順に実装する
   - 最後に MonthlyTab 既存コンポーネントに「KGI化」ボタンと KPI 一覧表示を追加する

5. **Refactor フェーズ** `/tsumiki:tdd-refactor`
   - フォームの state 管理パターンを統一する
   - エラーメッセージの共通化を検討する
   - `HabitLinkModal` の `availableHabits` の取得方法を MonthlyTab 側で整理する

6. **完了確認** `/tsumiki:tdd-verify-complete`
   - 全6テストケースが GREEN であることを確認する
   - `npm run build` でコンパイルエラーなしを確認する
   - `npm run lint` でリントエラーなしを確認する

---

## 信頼性レベルサマリー

- 🔵 **青信号**: 4項目 (67%)
- 🟡 **黄信号**: 2項目 (33%)

**品質評価**: 高品質（主要フォームのバリデーションロジックは要件定義に基づく。KPI 一覧の UI 詳細は実装時に調整余地あり）

## 注意事項

- 既存 MonthlyTab の実装ファイルパスは `frontend-v2/src/pages/` 内を確認してから変更する
- 既存 Goal 表示コンポーネントへの変更は最小限にとどめる（「KGI化」ボタン追加のみ）
- 目標ゴール最大3件制約（EDGE-KPI-001）はバックエンドが担保するため、フロントエンドでは制御不要
- `linkKpiHabits` は全上書き方式のため、送信前に「選択済み習慣が全て上書きされる」旨を UI で伝えることを推奨する（任意）
- KPI 削除確認は `window.confirm` を使うか、独自のダイアログコンポーネントを使うかは実装時に判断する（テスト容易性の観点から独自ダイアログを推奨）
