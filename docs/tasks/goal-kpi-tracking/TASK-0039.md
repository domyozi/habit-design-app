# TASK-0039: KGI/KPI E2E 統合テスト

**タスクID**: TASK-0039
**タスクタイプ**: TDD
**推定工数**: 8h
**フェーズ**: Phase 4 - 統合テスト
**信頼性レベル**: 🔵 *acceptance-criteria.md 統合テストシナリオより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/goal-kpi-tracking/requirements.md) - REQ-KGI-001〜007, REQ-KPI-001〜007, REQ-LOG-001〜005, REQ-DASH-001〜003
- **設計文書**: [📐 architecture.md](../../design/goal-kpi-tracking/architecture.md)
- **型定義**: [📝 interfaces.ts](../../design/goal-kpi-tracking/interfaces.ts)
- **API仕様**: [🔌 api-endpoints.md](../../design/goal-kpi-tracking/api-endpoints.md)

## タスク概要

KGI/KPI 機能全体を通した E2E 統合テストを Playwright で実装する。バックエンド・フロントエンドが起動した状態で実際のユーザー操作をシミュレートし、KGI 設定・KPI 作成・KPI ログ記録・グラフ表示の各フローが正常に動作することを確認する。

## 依存タスク

- **前提タスク**:
  - [TASK-0034: frontend-v2 型定義拡張 + APIクライアント追加](TASK-0034.md)
  - [TASK-0035: KgiCard コンポーネント実装](TASK-0035.md)
  - [TASK-0036: MonthlyTab KGI/KPI 設定 UI](TASK-0036.md)
  - [TASK-0037: KpiSection + KpiLogInput（今日のKPI入力）](TASK-0037.md)
  - [TASK-0038: KpiChart（Recharts グラフ表示）](TASK-0038.md)
- **後続タスク**: なし（フェーズ 4 の最終タスク）

## 完了条件

- [ ] `frontend-v2/tests/e2e/kgi-kpi.spec.ts` が作成されている
- [ ] KGI 設定フローの E2E テストが pass する（テストスイート1）
- [ ] KPI 作成・習慣連結フローの E2E テストが pass する（テストスイート2）
- [ ] KPI ログ記録フローの E2E テストが pass する（テストスイート3）
- [ ] KGI 現在値更新フローの E2E テストが pass する（テストスイート4）
- [ ] KPI グラフ表示フローの E2E テストが pass する（テストスイート5）
- [ ] `npm run test:e2e` (または相当コマンド) でテストスイートが実行できる

---

## 実装詳細（信頼性レベル付き）

### 前提条件の確認

**実装前に確認する項目**:

```bash
# 1. playwright.config.ts のベース URL 設定
cat frontend-v2/playwright.config.ts

# 2. 既存 E2E テストのパターン確認
ls frontend-v2/tests/e2e/

# 3. バックエンド起動確認
cd backend && uvicorn app.main:app --reload --port 8000

# 4. フロントエンド起動確認
cd frontend-v2 && npm run dev
```

---

### テストスイート1: KGI 設定フロー 🔵

**信頼性**: 🔵 *REQ-KGI-001〜003, acceptance-criteria より*

**実装ファイル**: `frontend-v2/tests/e2e/kgi-kpi.spec.ts`

```typescript
import { test, expect } from '@playwright/test';

test.describe('KGI 設定フロー', () => {
  test.beforeEach(async ({ page }) => {
    // シードデータが投入済みのテストユーザーでログイン
    await page.goto('/login');
    await page.fill('[data-testid="email-input"]', process.env.E2E_TEST_EMAIL ?? 'test@example.com');
    await page.fill('[data-testid="password-input"]', process.env.E2E_TEST_PASSWORD ?? 'testpass');
    await page.click('[data-testid="login-button"]');
    await page.waitForURL('**/dashboard');
  });

  test('既存のGoalをKGIとして設定し、KGIカードが表示される', async ({ page }) => {
    // MonthlyTab を開く
    await page.click('[data-testid="monthly-tab"]');

    // 既存 Goal の「KGI化」ボタンをクリック
    const goalCard = page.locator('[data-testid^="goal-card-"]').first();
    await goalCard.locator('[data-testid="set-kgi-button"]').click();

    // KGI 設定フォームに入力
    await page.fill('[data-testid="kgi-metric-type-select"]', 'numeric');
    await page.fill('[data-testid="kgi-target-value-input"]', '70');
    await page.fill('[data-testid="kgi-unit-input"]', 'kg');

    // target_date: 今日から6ヶ月後
    const sixMonthsLater = new Date();
    sixMonthsLater.setMonth(sixMonthsLater.getMonth() + 6);
    const targetDate = sixMonthsLater.toISOString().split('T')[0];
    await page.fill('[data-testid="kgi-target-date-input"]', targetDate);

    // 保存
    await page.click('[data-testid="kgi-save-button"]');

    // KGI カードが表示されることを確認
    await expect(page.locator('[data-testid^="kgi-card-"]')).toBeVisible({ timeout: 5000 });

    // プログレスバー・残り日数・目標値が表示されることを確認
    await expect(page.locator('[data-testid^="kgi-progress-bar-"]')).toBeVisible();
    await expect(page.locator('[data-testid^="kgi-days-remaining-"]')).toBeVisible();
    await expect(page.locator('[data-testid^="kgi-target-value-"]')).toContainText('70');
  });
});
```

---

### テストスイート2: KPI 作成・習慣連結フロー 🔵

**信頼性**: 🔵 *REQ-KPI-001〜005, REQ-KPI-007 より*

```typescript
test.describe('KPI 作成・習慣連結フロー', () => {
  test.beforeEach(async ({ page }) => {
    await loginAsTestUser(page); // ヘルパー関数
    await page.click('[data-testid="monthly-tab"]');
    // KGI が設定済みの Goal を前提とする（beforeAll でシードまたは事前設定）
  });

  test('KGIにKPIを追加し、習慣を連結できる', async ({ page }) => {
    // KGI カードから「KPIを追加」クリック
    const kgiCard = page.locator('[data-testid^="kgi-card-"]').first();
    await kgiCard.locator('[data-testid="add-kpi-button"]').click();

    // KPI フォームに入力
    await page.fill('[data-testid="kpi-title-input"]', '週の運動日数');
    await page.selectOption('[data-testid="kpi-metric-type-select"]', 'numeric');
    await page.fill('[data-testid="kpi-target-value-input"]', '4');
    await page.fill('[data-testid="kpi-unit-input"]', '回/週');
    await page.selectOption('[data-testid="kpi-tracking-frequency-select"]', 'weekly');

    // KPI 保存
    await page.click('[data-testid="kpi-form-save-button"]');

    // KPI が KGI 配下に表示されることを確認
    await expect(kgiCard.locator('[data-testid^="kpi-item-"]')).toContainText('週の運動日数');

    // 「関連習慣を選択」クリック
    await kgiCard.locator('[data-testid^="kpi-item-"]').first()
      .locator('[data-testid="link-habits-button"]').click();

    // 習慣を1件チェック
    const habitCheckbox = page.locator('[data-testid^="habit-option-"]').first();
    await habitCheckbox.click();

    // 保存
    await page.click('[data-testid="link-habits-save-button"]');

    // KPI に習慣名が表示されることを確認
    await expect(
      kgiCard.locator('[data-testid^="kpi-item-"]').first()
        .locator('[data-testid^="kpi-habit-link-"]'),
    ).toBeVisible();
  });
});
```

---

### テストスイート3: KPI ログ記録フロー 🔵

**信頼性**: 🔵 *REQ-LOG-001〜004, EDGE-KPI-004 より*

```typescript
test.describe('KPI ログ記録フロー', () => {
  test.beforeEach(async ({ page }) => {
    await loginAsTestUser(page);
    // 今日の画面（DailyTab / MorningTab）へ
    await page.click('[data-testid="daily-tab"]');
  });

  test('numeric KPI に数値を入力して「記録済み」になる', async ({ page }) => {
    const kpiSection = page.locator('[data-testid="kpi-section"]');
    await expect(kpiSection).toBeVisible({ timeout: 5000 });

    // numeric KPI の入力フィールドに値を入力
    const numericInput = kpiSection.locator('[data-testid^="kpi-numeric-input-"]').first();
    await numericInput.fill('4');

    // 「記録」ボタンをクリック
    const submitButton = kpiSection.locator('[data-testid^="kpi-submit-button-"]').first();
    await submitButton.click();

    // 「記録済み」表示に切り替わることを確認
    await expect(kpiSection.locator('[data-testid^="kpi-log-completed-"]').first()).toBeVisible({ timeout: 3000 });
  });

  test('binary KPI のチェックボックスをクリックすると即時「記録済み」になる', async ({ page }) => {
    const kpiSection = page.locator('[data-testid="kpi-section"]');
    await expect(kpiSection).toBeVisible({ timeout: 5000 });

    const binaryCheckbox = kpiSection.locator('[data-testid^="kpi-binary-checkbox-"]').first();
    await binaryCheckbox.click();

    // 即時に完了表示になることを確認
    await expect(
      kpiSection.locator('[data-testid^="kpi-log-completed-"]').first(),
    ).toBeVisible({ timeout: 3000 });
  });

  test('percentage KPI に 101 を入力するとエラーメッセージが表示される', async ({ page }) => {
    const kpiSection = page.locator('[data-testid="kpi-section"]');
    await expect(kpiSection).toBeVisible({ timeout: 5000 });

    const percentageInput = kpiSection.locator('[data-testid^="kpi-percentage-input-"]').first();
    await percentageInput.fill('101');

    // エラーメッセージが表示されることを確認
    await expect(
      kpiSection.locator('[data-testid^="kpi-percentage-error-"]').first(),
    ).toContainText('0〜100の値を入力してください');

    // 「記録」ボタンが disabled であることを確認
    await expect(
      kpiSection.locator('[data-testid^="kpi-submit-button-"]').first(),
    ).toBeDisabled();
  });
});
```

---

### テストスイート4: KGI 現在値更新フロー 🔵

**信頼性**: 🔵 *REQ-KGI-004, REQ-KGI-005 より*

```typescript
test.describe('KGI 現在値更新フロー', () => {
  test.beforeEach(async ({ page }) => {
    await loginAsTestUser(page);
    await page.click('[data-testid="monthly-tab"]');
  });

  test('KGI の現在値を更新するとカードとプログレスバーが更新される', async ({ page }) => {
    // 「現在値を更新」ボタンをクリック
    const kgiCard = page.locator('[data-testid^="kgi-card-"]').first();
    await kgiCard.locator('[data-testid="update-current-value-button"]').click();

    // モーダルが表示されることを確認
    const modal = page.locator('[data-testid="update-kgi-modal"]');
    await expect(modal).toBeVisible();

    // 新しい現在値を入力
    await modal.locator('[data-testid="kgi-current-value-input"]').fill('74.5');
    await modal.locator('[data-testid="kgi-current-value-save-button"]').click();

    // モーダルが閉じる
    await expect(modal).not.toBeVisible({ timeout: 3000 });

    // KGI カードの現在値が更新されることを確認
    await expect(kgiCard.locator('[data-testid^="kgi-current-value-"]')).toContainText('74.5');

    // プログレスバーの% が更新されていることを確認（target_value=70 の場合、達成率 100% 超）
    await expect(kgiCard.locator('[data-testid^="kgi-progress-bar-"]')).toBeVisible();
  });
});
```

---

### テストスイート5: KPI グラフ表示フロー 🔵

**信頼性**: 🔵 *REQ-LOG-005, REQ-DASH-003 より*

```typescript
test.describe('KPI グラフ表示フロー', () => {
  test.beforeEach(async ({ page }) => {
    await loginAsTestUser(page);
    await page.click('[data-testid="monthly-tab"]');
  });

  test('KPIカードを展開するとグラフが表示され、タブ切り替えができる', async ({ page }) => {
    // KGI カード内の KPI アイテムをクリック（展開）
    const kpiItem = page.locator('[data-testid^="kpi-item-"]').first();
    await kpiItem.click();

    // KpiChart が表示されることを確認
    const kpiChart = page.locator('[data-testid="kpi-chart"]');
    await expect(kpiChart).toBeVisible({ timeout: 5000 });

    // 「週次」タブをクリック
    await kpiChart.locator('[data-testid="kpi-chart-tab-weekly"]').click();

    // 棒グラフ or 週次グラフが表示されることを確認（ローディング後）
    await expect(kpiChart.locator('[data-testid="kpi-chart-loading"]')).not.toBeVisible({ timeout: 5000 });
    // 空 or グラフ表示
    const graphOrEmpty = kpiChart.locator('[data-testid="kpi-chart-graph"], [data-testid="kpi-chart-empty"]');
    await expect(graphOrEmpty).toBeVisible();

    // 「月次」タブをクリック
    await kpiChart.locator('[data-testid="kpi-chart-tab-monthly"]').click();
    await expect(kpiChart.locator('[data-testid="kpi-chart-loading"]')).not.toBeVisible({ timeout: 5000 });
    await expect(graphOrEmpty).toBeVisible();
  });
});
```

---

### テストスイート6: エラーハンドリング 🟡

**信頼性**: 🟡 *堅牢性要件から妥当な推測*

```typescript
test.describe('エラーハンドリング', () => {
  test.beforeEach(async ({ page }) => {
    await loginAsTestUser(page);
    await page.click('[data-testid="monthly-tab"]');
  });

  test('KGI設定フォームで target_date 未入力のまま送信するとエラーが表示される', async ({ page }) => {
    const goalCard = page.locator('[data-testid^="goal-card-"]').first();
    await goalCard.locator('[data-testid="set-kgi-button"]').click();

    // target_date を入力せずに保存
    await page.fill('[data-testid="kgi-target-value-input"]', '70');
    await page.click('[data-testid="kgi-save-button"]');

    // バリデーションエラーが表示されることを確認
    await expect(page.locator('[data-testid="kgi-target-date-error"]')).toBeVisible();
  });

  test('APIネットワークエラー時にエラートーストが表示される', async ({ page, context }) => {
    // ネットワークエラーをシミュレート
    await context.route('**/api/**', (route) => route.abort('failed'));

    // KPI 記録を試みる
    await page.click('[data-testid="daily-tab"]');
    const kpiSection = page.locator('[data-testid="kpi-section"]');

    // エラー状態 or エラートーストが表示されることを確認
    await expect(
      page.locator('[data-testid="kpi-section-error"], [data-testid="error-toast"]'),
    ).toBeVisible({ timeout: 5000 });
  });
});
```

---

### ヘルパー関数

```typescript
// frontend-v2/tests/e2e/helpers/auth.ts

import type { Page } from '@playwright/test';

export async function loginAsTestUser(page: Page): Promise<void> {
  await page.goto('/login');
  await page.fill(
    '[data-testid="email-input"]',
    process.env.E2E_TEST_EMAIL ?? 'test@example.com',
  );
  await page.fill(
    '[data-testid="password-input"]',
    process.env.E2E_TEST_PASSWORD ?? 'testpass',
  );
  await page.click('[data-testid="login-button"]');
  await page.waitForURL('**/dashboard');
}
```

---

## 統合テスト要件

### 環境要件

| 項目 | 要件 |
|---|---|
| バックエンド | `localhost:8000` で起動済み |
| フロントエンド | `localhost:5173` で起動済み (`npm run dev`) |
| Supabase | テスト用プロジェクト or ローカル Supabase |
| テストユーザー | ログイン済みセッション or シードデータ |
| 既存データ | `Goal` が1件以上存在するテストユーザー |

### シードデータ要件

E2E テストが安定して動作するために、以下のデータを事前に用意する（または `beforeAll` で作成する）:

1. テストユーザーアカウント（`E2E_TEST_EMAIL` / `E2E_TEST_PASSWORD`）
2. テストユーザーに紐づく `Goal` が1件以上
3. テストユーザーに紐づく `Habit` が1件以上（習慣連結テスト用）
4. KPI ログが1件以上（グラフ表示テスト用に任意）

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-0039` - E2E テスト仕様の確認
2. `/tsumiki:tdd-testcases` - テストケースの詳細化
3. 既存 E2E テストのパターン確認
   ```bash
   ls /Users/tsukasaakiyama/aiwork/habit-design-app/frontend-v2/tests/e2e/
   cat /Users/tsukasaakiyama/aiwork/habit-design-app/frontend-v2/playwright.config.ts
   ```
4. `/tsumiki:tdd-red` - `kgi-kpi.spec.ts` を実装（失敗状態）
5. バックエンド・フロントエンドを起動し、シードデータを投入
   ```bash
   # バックエンド起動
   cd /Users/tsukasaakiyama/aiwork/habit-design-app/backend
   uvicorn app.main:app --reload --port 8000

   # フロントエンド起動
   cd /Users/tsukasaakiyama/aiwork/habit-design-app/frontend-v2
   npm run dev
   ```
6. `/tsumiki:tdd-green` - テストが pass するまでデバッグ・修正
7. `/tsumiki:tdd-verify-complete` - 全テストスイートの確認

---

## 注意事項

- E2E テストは実際の Supabase（またはテスト用モックサーバー）に接続する。ローカル Supabase を使う場合は `.env.test` に接続先を設定する
- `playwright.config.ts` の `baseURL` が `http://localhost:5173` に設定されていることを確認する
- 既存の E2E テスト (`frontend-v2/tests/e2e/`) のパターンを踏襲して命名・構造を揃える
- テストの独立性を保つため、各 `test.describe` は専用のシードデータに依存しないよう設計する
- `page.waitForURL` や `expect(...).toBeVisible({ timeout: ... })` で非同期処理の完了を待機する
- CI 環境では `E2E_TEST_EMAIL` / `E2E_TEST_PASSWORD` を環境変数として設定する
- `context.route` を使ったネットワークエラーのシミュレートはテストスイート6のみに限定する（他テストへの干渉を防ぐため `afterEach` でルートを解除する）

---

## 信頼性レベルサマリー

- **総項目数**: 12項目
- 🔵 **青信号**: 10項目 (83%)
- 🟡 **黄信号**: 2項目 (17%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質（受け入れ基準・要件定義に基づく統合テスト）
