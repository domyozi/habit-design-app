# TASK-0031: kpis.py KPI CRUD + KPIログupsert API実装

**タスクID**: TASK-0031
**タスクタイプ**: TDD
**推定工数**: 8時間
**フェーズ**: Phase 2 - バックエンド実装
**信頼性レベル**: 🔵 *REQ-KPI-001〜007・REQ-LOG-001〜004・api-endpoints.md より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/goal-kpi-tracking/requirements.md) - REQ-KPI-001〜007, REQ-LOG-001〜004, EDGE-KPI-003, EDGE-KPI-006, EDGE-KPI-007
- **API仕様**: [🔌 api-endpoints.md](../../design/goal-kpi-tracking/api-endpoints.md)

## タスク概要

新規ファイル `backend/app/api/routes/kpis.py` を作成する。
- `POST /kpis` - KPI 作成
- `GET /kpis?goal_id={id}` - KGI に紐付く KPI 一覧取得
- `GET /kpis/today` - 今日の KPI 一覧（記録状況付き）
- `PUT /kpis/{id}/logs` - KPI ログ upsert（手動入力）
- `POST /kpis/{id}/habits` - KPI 習慣連結（全上書き）
- `DELETE /kpis/{id}` - KPI 削除

## 依存タスク

- **前提タスク**: [TASK-0030: goals.py KGI API](TASK-0030.md)
- **後続タスク**: [TASK-0032: KPIログ集計API](TASK-0032.md), [TASK-0033: 音声/AI統合](TASK-0033.md)

## 完了条件

- [ ] `POST /kpis` が正常に動作する（Goal が KGI でない場合は 422）
- [ ] `GET /kpis/today` が今日のログ状況付きで返る
- [ ] `PUT /kpis/{id}/logs` が upsert（同日上書き）で動作する
- [ ] `percentage` 型で 0〜100 外の値が 422 エラーになる
- [ ] `binary` 型で 0.0/1.0 以外が 422 エラーになる
- [ ] `POST /kpis/{id}/habits` が全上書きで連結できる
- [ ] 全 TDD テストが通過している

---

## 実装詳細

### 1. kpis.py ルーター作成 🔵

**実装ファイル**: `backend/app/api/routes/kpis.py`（新規）

```python
from fastapi import APIRouter, Depends, HTTPException, Query
from supabase import Client
from app.core.supabase import get_supabase_client
from app.core.security import get_current_user_id
from app.models.schemas import (
    KpiCreate, KpiResponse, KpiWithTodayStatus,
    KpiLogUpsert, KpiLogResponse, LinkKpiHabitsRequest
)
from datetime import date

router = APIRouter(prefix="/kpis", tags=["kpis"])
```

### 2. KPI 作成エンドポイント 🔵

**信頼性**: 🔵 *REQ-KPI-001 より*

```python
@router.post("", response_model=KpiResponse)
async def create_kpi(
    request: KpiCreate,
    user_id: str = Depends(get_current_user_id),
    supabase: Client = Depends(get_supabase_client),
):
    """KPI を作成する。goal_id の Goal が KGI であること（target_date 設定済み）が前提"""
    # goal が KGI かを確認
    goal = supabase.table("goals").select("target_date").eq("id", request.goal_id).eq("user_id", user_id).single().execute()
    if not goal.data or not goal.data.get("target_date"):
        raise HTTPException(status_code=422, detail="指定した Goal は KGI として設定されていません")
    
    data = {**request.model_dump(), "user_id": user_id}
    result = supabase.table("kpis").insert(data).execute()
    return result.data[0]
```

### 3. 今日の KPI 一覧エンドポイント 🔵

**信頼性**: 🔵 *REQ-DASH-002 より*

```python
@router.get("/today", response_model=list[KpiWithTodayStatus])
async def get_today_kpis(
    user_id: str = Depends(get_current_user_id),
    supabase: Client = Depends(get_supabase_client),
):
    """今日の KPI 一覧を記録状況付きで返す"""
    today = str(date.today())
    
    # アクティブな KPI 取得
    kpis = supabase.table("kpis").select("*").eq("user_id", user_id).eq("is_active", True).execute()
    
    result = []
    for kpi in kpis.data:
        # 今日のログ取得
        log = supabase.table("kpi_logs").select("*").eq("kpi_id", kpi["id"]).eq("log_date", today).single().execute()
        today_log = log.data
        
        # 紐付き習慣取得
        habits = supabase.table("kpi_habits").select("habit_id, habits(title)").eq("kpi_id", kpi["id"]).execute()
        
        result.append(KpiWithTodayStatus(
            **kpi,
            habit_ids=[h["habit_id"] for h in habits.data],
            today_completed=today_log is not None and today_log.get("value") in [1.0] if kpi["metric_type"] == "binary" else today_log is not None,
            today_value=today_log.get("value") if today_log else None,
            connected_habits=[{"habit_id": h["habit_id"], "habit_title": h.get("habits", {}).get("title")} for h in habits.data],
        ))
    
    return result
```

### 4. KPI ログ upsert エンドポイント 🔵

**信頼性**: 🔵 *REQ-LOG-002・EDGE-KPI-007 より*

```python
@router.put("/{kpi_id}/logs", response_model=KpiLogResponse)
async def upsert_kpi_log(
    kpi_id: str,
    request: KpiLogUpsert,
    user_id: str = Depends(get_current_user_id),
    supabase: Client = Depends(get_supabase_client),
):
    """KPI ログを upsert する（同日に記録済みの場合は上書き）"""
    # KPI の metric_type を取得してバリデーション
    kpi = supabase.table("kpis").select("metric_type").eq("id", kpi_id).eq("user_id", user_id).single().execute()
    if not kpi.data:
        raise HTTPException(status_code=404, detail="KPI not found")
    
    metric_type = kpi.data["metric_type"]
    value = request.value
    
    # バリデーション
    if metric_type == "percentage" and not (0 <= value <= 100):
        raise HTTPException(status_code=422, detail="percentage 型の値は 0〜100 の範囲で入力してください")
    if metric_type == "binary" and value not in [0.0, 1.0]:
        raise HTTPException(status_code=422, detail="binary 型の値は 0.0（未達成）または 1.0（達成）のみ有効です")
    
    data = {
        "kpi_id": kpi_id,
        "user_id": user_id,
        "log_date": str(request.log_date),
        "value": value,
        "input_method": request.input_method or "manual",
        "note": request.note,
    }
    
    # upsert（UNIQUE (kpi_id, log_date) で上書き）
    result = supabase.table("kpi_logs").upsert(data, on_conflict="kpi_id,log_date").execute()
    return result.data[0]
```

### 5. KPI 習慣連結エンドポイント 🔵

**信頼性**: 🔵 *REQ-KPI-006・REQ-KPI-007 より*

```python
@router.post("/{kpi_id}/habits")
async def link_kpi_habits(
    kpi_id: str,
    request: LinkKpiHabitsRequest,
    user_id: str = Depends(get_current_user_id),
    supabase: Client = Depends(get_supabase_client),
):
    """KPI に習慣を連結する（全上書き方式）"""
    # 既存の連結を全削除
    supabase.table("kpi_habits").delete().eq("kpi_id", kpi_id).eq("user_id", user_id).execute()
    
    # 新しい連結を挿入
    if request.habit_ids:
        rows = [{"kpi_id": kpi_id, "habit_id": hid, "user_id": user_id} for hid in request.habit_ids]
        supabase.table("kpi_habits").insert(rows).execute()
    
    return {"success": True, "data": {"kpi_id": kpi_id, "habit_ids": request.habit_ids}}
```

### 6. main.py へのルーター登録 🔵

**実装ファイル**: `backend/app/main.py`

```python
from app.api.routes import kpis
app.include_router(kpis.router, prefix="/api/v1")
```

---

## 単体テスト要件

### テスト1: KPI 作成の正常系 🔵

```python
def test_create_kpi_success(client, auth_headers, kgi_goal_id):
    response = client.post("/kpis", json={
        "goal_id": kgi_goal_id,
        "title": "週の運動日数",
        "metric_type": "numeric",
        "target_value": 4,
        "unit": "回/週",
        "tracking_frequency": "weekly"
    }, headers=auth_headers)
    assert response.status_code == 200
    assert response.json()["data"]["title"] == "週の運動日数"
```

### テスト2: KPI ログ upsert + 同日上書き 🔵

```python
def test_upsert_kpi_log_overwrite(client, auth_headers, kpi_id):
    # 1回目
    client.put(f"/kpis/{kpi_id}/logs", json={"log_date": "2026-04-15", "value": 74.5}, headers=auth_headers)
    # 2回目（同日上書き）
    response = client.put(f"/kpis/{kpi_id}/logs", json={"log_date": "2026-04-15", "value": 74.8}, headers=auth_headers)
    assert response.status_code == 200
    assert response.json()["data"]["value"] == 74.8
```

### テスト3: percentage 型の範囲バリデーション 🔵

```python
def test_kpi_log_percentage_out_of_range(client, auth_headers, percentage_kpi_id):
    response = client.put(f"/kpis/{percentage_kpi_id}/logs", json={"log_date": "2026-04-15", "value": 105}, headers=auth_headers)
    assert response.status_code == 422
```

### テスト4: binary 型の値バリデーション 🔵

```python
def test_kpi_log_binary_invalid_value(client, auth_headers, binary_kpi_id):
    response = client.put(f"/kpis/{binary_kpi_id}/logs", json={"log_date": "2026-04-15", "value": 0.5}, headers=auth_headers)
    assert response.status_code == 422
```

---

## 実装手順（TDD）

1. `/tsumiki:tdd-requirements TASK-0031`
2. `/tsumiki:tdd-testcases`
3. `/tsumiki:tdd-red`
4. `/tsumiki:tdd-green`
5. `/tsumiki:tdd-refactor`
6. `/tsumiki:tdd-verify-complete`

---

## 信頼性レベルサマリー

- 🔵 **青信号**: 9項目 (100%)

**品質評価**: 高品質
