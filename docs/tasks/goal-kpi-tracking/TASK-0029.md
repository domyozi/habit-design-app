# TASK-0029: schemas.py KGI/KPI Pydanticモデル追加

**タスクID**: TASK-0029
**タスクタイプ**: DIRECT
**推定工数**: 4時間
**フェーズ**: Phase 1 - データベース基盤
**信頼性レベル**: 🔵 *interfaces.ts・要件定義書・ヒアリングより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/goal-kpi-tracking/requirements.md) - REQ-KGI-001〜007, REQ-KPI-001〜007, REQ-LOG-001〜004
- **設計文書**: [📐 architecture.md](../../design/goal-kpi-tracking/architecture.md)
- **型定義参照**: [📝 interfaces.ts](../../design/goal-kpi-tracking/interfaces.ts)

## タスク概要

`backend/app/models/schemas.py` に KGI/KPI 関連の Pydantic モデルを追加する。
`interfaces.ts` の型定義と対応するモデルを定義し、
FastAPI のリクエスト/レスポンス検証に使用できるようにする。

## 依存タスク

- **前提タスク**: [TASK-0028: DBスキーマ適用](TASK-0028.md)
- **後続タスク**: [TASK-0030: goals.py KGI API](TASK-0030.md), [TASK-0031: kpis.py KPI API](TASK-0031.md)

## 完了条件

- [ ] `MetricType` Literal 型が定義されている
- [ ] `TrackingFrequency` Literal 型が定義されている
- [ ] `KpiInputMethod` Literal 型が定義されている
- [ ] `GoalWithKgiResponse` モデルが定義されている（達成率・残り日数の計算フィールド含む）
- [ ] `SetKgiRequest`・`UpdateKgiCurrentValueRequest` モデルが定義されている
- [ ] `KpiCreate`・`KpiResponse`・`KpiWithTodayStatus` モデルが定義されている
- [ ] `KpiLogUpsert`・`KpiLogResponse` モデルが定義されている
- [ ] `KpiChartResponse`・`KpiChartDataPoint` モデルが定義されている
- [ ] `LinkKpiHabitsRequest` モデルが定義されている
- [ ] Python の型チェック（mypy または FastAPI の起動確認）が通ること

---

## 実装詳細

### 1. 共通 Literal 型の追加 🔵

**信頼性**: 🔵 *interfaces.ts の MetricType・TrackingFrequency・KpiInputMethod より*

**実装ファイル**: `backend/app/models/schemas.py`

```python
from typing import Literal, Optional
from datetime import date, datetime
from pydantic import BaseModel, Field, field_validator

# KGI/KPI 共通型
MetricType = Literal["numeric", "percentage", "binary"]
TrackingFrequency = Literal["daily", "weekly", "monthly"]
KpiInputMethod = Literal["manual", "voice", "auto"]
```

### 2. KGI リクエスト/レスポンスモデル 🔵

**信頼性**: 🔵 *REQ-KGI-001〜007・interfaces.ts より*

```python
class SetKgiRequest(BaseModel):
    """既存 Goal を KGI として設定するリクエスト"""
    target_value: Optional[float] = None  # numeric/percentage 型で推奨
    unit: Optional[str] = Field(None, max_length=20)
    target_date: date  # 必須: REQ-KGI-002
    metric_type: MetricType  # 必須: REQ-KGI-003
    current_value: Optional[float] = None

    @field_validator("target_date")
    @classmethod
    def validate_target_date(cls, v):
        """target_date は必須（バリデーション済み）"""
        return v

    @field_validator("target_value")
    @classmethod
    def validate_percentage_range(cls, v, info):
        """percentage 型の場合は 0〜100 の範囲"""
        if info.data.get("metric_type") == "percentage" and v is not None:
            if not (0 <= v <= 100):
                raise ValueError("percentage 型の target_value は 0〜100 の範囲で入力してください")
        return v


class UpdateKgiCurrentValueRequest(BaseModel):
    """KGI 現在値更新リクエスト"""
    current_value: float  # REQ-KGI-005


class GoalWithKgiResponse(BaseModel):
    """KGI 属性を含む Goal レスポンス"""
    id: str
    user_id: str
    wanna_be_id: Optional[str] = None
    title: str
    description: Optional[str] = None
    display_order: int
    is_active: bool
    # KGI 拡張フィールド
    target_value: Optional[float] = None
    current_value: Optional[float] = None
    unit: Optional[str] = None
    target_date: Optional[date] = None
    metric_type: Optional[MetricType] = None
    # サーバー計算フィールド
    achievement_rate: Optional[float] = None  # REQ-KGI-006
    days_remaining: Optional[int] = None  # REQ-KGI-007
    is_expired: bool = False  # EDGE-KPI-005
    is_kgi: bool = False  # target_date IS NOT NULL
    created_at: datetime
    updated_at: datetime
```

### 3. KPI モデル 🔵

**信頼性**: 🔵 *REQ-KPI-001〜007・interfaces.ts の Kpi より*

```python
class KpiCreate(BaseModel):
    """KPI 作成リクエスト"""
    goal_id: str  # REQ-KPI-001
    title: str = Field(..., max_length=200)
    description: Optional[str] = None
    metric_type: MetricType  # REQ-KPI-002
    target_value: Optional[float] = None  # REQ-KPI-004
    unit: Optional[str] = Field(None, max_length=20)  # REQ-KPI-004
    tracking_frequency: TrackingFrequency  # REQ-KPI-003
    display_order: int = 0


class KpiResponse(BaseModel):
    """KPI レスポンス"""
    id: str
    user_id: str
    goal_id: str
    title: str
    description: Optional[str] = None
    metric_type: MetricType
    target_value: Optional[float] = None
    unit: Optional[str] = None
    tracking_frequency: TrackingFrequency
    display_order: int
    is_active: bool
    created_at: datetime
    updated_at: datetime
    habit_ids: list[str] = []  # REQ-KPI-006: JOIN フィールド


class KpiWithTodayStatus(KpiResponse):
    """今日のKPI状態（MorningTab 用）"""
    today_completed: bool  # REQ-DASH-002
    today_value: Optional[float] = None
    connected_habits: list[dict] = []  # {habit_id, habit_title}


class LinkKpiHabitsRequest(BaseModel):
    """KPI 習慣連結リクエスト（全上書き方式）"""
    habit_ids: list[str]  # REQ-KPI-006, REQ-KPI-007
```

### 4. KPI ログモデル 🔵

**信頼性**: 🔵 *REQ-LOG-001〜004・EDGE-KPI-007 より*

```python
class KpiLogUpsert(BaseModel):
    """KPI ログ upsert リクエスト"""
    log_date: date  # 記録日
    value: float  # REQ-LOG-002: binary型: 1.0=達成, 0.0=未達成
    input_method: Optional[KpiInputMethod] = "manual"
    note: Optional[str] = Field(None, max_length=500)

    @field_validator("value")
    @classmethod
    def validate_value(cls, v, info):
        """metric_type に応じたバリデーションはルート側で実施"""
        return v


class KpiLogResponse(BaseModel):
    """KPI ログレスポンス"""
    id: str
    kpi_id: str
    user_id: str
    log_date: date
    value: float
    input_method: Optional[KpiInputMethod] = None
    note: Optional[str] = None
    created_at: datetime
```

### 5. グラフ集計モデル 🔵

**信頼性**: 🔵 *REQ-LOG-005・interfaces.ts の KpiChartResponse より*

```python
class KpiChartDataPoint(BaseModel):
    """グラフデータポイント"""
    date: str  # 日次: YYYY-MM-DD / 週次: 週の開始日 / 月次: YYYY-MM
    value: Optional[float] = None  # 記録なしの場合 null


class KpiChartSummary(BaseModel):
    avg: Optional[float] = None
    max: Optional[float] = None
    min: Optional[float] = None
    latest_value: Optional[float] = None
    target_value: Optional[float] = None


class KpiChartResponse(BaseModel):
    """KPI グラフレスポンス"""
    kpi_id: str
    granularity: Literal["daily", "weekly", "monthly"]
    data_points: list[KpiChartDataPoint]
    summary: KpiChartSummary
```

---

## 単体テスト要件

### テスト1: SetKgiRequest バリデーション 🔵

**信頼性**: 🔵 *REQ-KGI-002 より*

```python
def test_set_kgi_request_target_date_required():
    """target_date は必須"""
    with pytest.raises(ValidationError):
        SetKgiRequest(metric_type="numeric", target_value=70, unit="kg")
        # target_date がないので ValidationError
```

### テスト2: percentage 型範囲バリデーション 🔵

**信頼性**: 🔵 *EDGE-KPI-004 より*

```python
def test_set_kgi_request_percentage_range():
    """percentage 型の target_value は 0〜100 の範囲"""
    with pytest.raises(ValidationError):
        SetKgiRequest(
            metric_type="percentage",
            target_value=105,
            target_date=date(2026, 10, 15)
        )
```

### テスト3: KpiCreate バリデーション 🔵

**信頼性**: 🔵 *REQ-KPI-001〜005 より*

```python
def test_kpi_create_valid():
    kpi = KpiCreate(
        goal_id="test-uuid",
        title="週の運動日数",
        metric_type="numeric",
        target_value=4,
        unit="回/週",
        tracking_frequency="weekly"
    )
    assert kpi.tracking_frequency == "weekly"
```

---

## 実装手順（DIRECT）

1. `/tsumiki:direct-setup` - `backend/app/models/schemas.py` に上記モデルを追加
2. `/tsumiki:direct-verify` - `cd backend && python -c "from app.models.schemas import *; print('OK')"` で import エラーがないことを確認

---

## 注意事項

- 既存の `schemas.py` の末尾に追加する（既存モデルを変更しない）
- 既存の `Goal`・`GoalCreate` モデルとは別に `GoalWithKgiResponse` を新規定義する
- `achievement_rate` の計算ロジックはルートハンドラで実装する（schema では Optional[float] として受け取る）

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 5 | 0 | 0 | 5 |
| 単体テスト | 3 | 0 | 0 | 3 |

- 🔵 **青信号**: 8項目 (100%)

**品質評価**: 高品質
