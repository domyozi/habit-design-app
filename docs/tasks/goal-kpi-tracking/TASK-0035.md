# TASK-0035: KgiCard コンポーネント実装

**タスクID**: TASK-0035
**タスクタイプ**: TDD
**推定工数**: 8h
**フェーズ**: Phase 3 - フロントエンド実装
**信頼性レベル**: 🔵 *REQ-DASH-001・dataflow.md より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/goal-kpi-tracking/requirements.md) - REQ-KGI-004〜007, REQ-DASH-001, EDGE-KPI-005
- **設計文書**: [📐 architecture.md](../../design/goal-kpi-tracking/architecture.md)
- **型定義**: [📝 interfaces.ts](../../design/goal-kpi-tracking/interfaces.ts)
- **API仕様**: [🔌 api-endpoints.md](../../design/goal-kpi-tracking/api-endpoints.md)

## タスク概要

`GoalWithKgi` 型に対応した `KgiCard` コンポーネントを実装する。通常の Goal（`is_kgi=false`）と KGI（`is_kgi=true`）の両方を表示でき、KGI の場合はプログレスバー・達成率・残り日数・期限超過バッジ・現在値更新ボタンを表示する。metric_type ごとに表示形式を切り替える。

## 依存タスク

- **前提タスク**: [TASK-0034: frontend-v2 型定義拡張 + APIクライアント追加](TASK-0034.md)
- **後続タスク**: [TASK-0036: MonthlyTab KGI/KPI 設定 UI](TASK-0036.md)

## 完了条件

- [ ] `frontend-v2/src/components/KgiCard.tsx` が実装されている
- [ ] `frontend-v2/src/components/KgiCard.test.tsx` が実装されている
- [ ] `is_kgi=false/true` の両ケースで正常にレンダリングされる
- [ ] `metric_type` ごとの表示が正しい（numeric / percentage / binary）
- [ ] `is_expired=true` の場合に「期限超過」バッジが表示される
- [ ] `days_remaining` が正負両方のケースで正しいテキストが表示される
- [ ] TDD で全テストケースが通過している

---

## 実装詳細

### 1. KgiCard コンポーネント 🔵

**信頼性**: 🔵 *REQ-DASH-001 より*

`GoalWithKgi` を受け取り、`is_kgi` フラグで表示を切り替える React コンポーネント。

**実装ファイル**: `frontend-v2/src/components/KgiCard.tsx`

```typescript
import React, { useState } from 'react';
import { GoalWithKgi, MetricType } from '../types';

export interface KgiCardProps {
  goal: GoalWithKgi;
  onCurrentValueUpdate?: (goalId: string, value: number) => void;
}

// ─── 補助関数 ───────────────────────────────────────────────────────────────

function formatValue(value: number, metricType: MetricType, unit?: string): string {
  if (metricType === 'binary') {
    return value >= 1 ? '達成済み' : '未達成';
  }
  const formatted = metricType === 'percentage'
    ? `${value.toFixed(1)}%`
    : `${value}`;
  return unit ? `${formatted} ${unit}` : formatted;
}

function getDaysRemainingLabel(daysRemaining: number): string {
  if (daysRemaining >= 0) {
    return `残り ${daysRemaining} 日`;
  }
  return `${Math.abs(daysRemaining)} 日超過`;
}

// ─── サブコンポーネント: 現在値更新ダイアログ ─────────────────────────────

interface UpdateDialogProps {
  goal: GoalWithKgi;
  onSave: (value: number) => void;
  onClose: () => void;
}

function UpdateDialog({ goal, onSave, onClose }: UpdateDialogProps) {
  const [inputValue, setInputValue] = useState<string>(
    goal.current_value?.toString() ?? '0'
  );

  const handleSave = () => {
    const parsed = parseFloat(inputValue);
    if (!isNaN(parsed)) {
      onSave(parsed);
    }
  };

  if (goal.metric_type === 'binary') {
    return (
      <div role="dialog" aria-modal="true" aria-label="現在値を更新">
        <p>目標を達成しましたか？</p>
        <button onClick={() => onSave(1.0)}>達成済み</button>
        <button onClick={() => onSave(0.0)}>未達成</button>
        <button onClick={onClose}>キャンセル</button>
      </div>
    );
  }

  return (
    <div role="dialog" aria-modal="true" aria-label="現在値を更新">
      <label htmlFor="current-value-input">現在値</label>
      <input
        id="current-value-input"
        type="number"
        value={inputValue}
        onChange={(e) => setInputValue(e.target.value)}
        min={goal.metric_type === 'percentage' ? 0 : undefined}
        max={goal.metric_type === 'percentage' ? 100 : undefined}
      />
      {goal.unit && <span>{goal.unit}</span>}
      <button onClick={handleSave}>保存</button>
      <button onClick={onClose}>キャンセル</button>
    </div>
  );
}

// ─── メインコンポーネント ──────────────────────────────────────────────────

export function KgiCard({ goal, onCurrentValueUpdate }: KgiCardProps) {
  const [isDialogOpen, setIsDialogOpen] = useState(false);

  const handleUpdateClick = () => setIsDialogOpen(true);

  const handleSave = (value: number) => {
    onCurrentValueUpdate?.(goal.id, value);
    setIsDialogOpen(false);
  };

  // ── 通常 Goal（KGI ではない）の表示 ────────────────────────────────────
  if (!goal.is_kgi) {
    return (
      <div className="goal-card">
        <h3>{goal.title}</h3>
        {goal.description && <p>{goal.description}</p>}
      </div>
    );
  }

  // ── KGI の表示 ──────────────────────────────────────────────────────────
  const achievementRate = goal.achievement_rate ?? 0;
  const daysRemaining = goal.days_remaining ?? 0;

  return (
    <div className="goal-card kgi-card">
      {/* ヘッダー */}
      <div className="kgi-card__header">
        <h3>{goal.title}</h3>
        {goal.is_expired && (
          <span className="kgi-card__badge kgi-card__badge--expired">
            期限超過
          </span>
        )}
      </div>

      {/* 達成率プログレスバー */}
      <div className="kgi-card__progress" aria-label="達成率">
        <div
          className="kgi-card__progress-bar"
          style={{ width: `${Math.min(achievementRate, 100)}%` }}
          role="progressbar"
          aria-valuenow={achievementRate}
          aria-valuemin={0}
          aria-valuemax={100}
        />
        <span className="kgi-card__progress-label">{achievementRate.toFixed(0)}%</span>
      </div>

      {/* 現在値 / 目標値 */}
      <div className="kgi-card__values">
        <span className="kgi-card__current">
          {formatValue(goal.current_value ?? 0, goal.metric_type!, goal.unit)}
        </span>
        {goal.metric_type !== 'binary' && goal.target_value !== undefined && (
          <>
            <span className="kgi-card__separator"> / </span>
            <span className="kgi-card__target">
              {formatValue(goal.target_value, goal.metric_type!, goal.unit)}
            </span>
          </>
        )}
      </div>

      {/* 残り日数 */}
      {goal.days_remaining !== undefined && (
        <div className="kgi-card__days">
          {getDaysRemainingLabel(daysRemaining)}
        </div>
      )}

      {/* 現在値更新ボタン */}
      <button
        className="kgi-card__update-button"
        onClick={handleUpdateClick}
      >
        現在値を更新
      </button>

      {/* 更新ダイアログ */}
      {isDialogOpen && (
        <UpdateDialog
          goal={goal}
          onSave={handleSave}
          onClose={() => setIsDialogOpen(false)}
        />
      )}
    </div>
  );
}

export default KgiCard;
```

---

## 単体テスト要件

### テストケース1: `is_kgi=false` では KGI プログレスバーが表示されない 🔵

**Given**: `goal.is_kgi = false` の `GoalWithKgi` オブジェクト
**When**: `KgiCard` をレンダリングする
**Then**: プログレスバー要素（`role="progressbar"`）が DOM に存在しない（REQ-DASH-001）

### テストケース2: `is_kgi=true, achievement_rate=72` でプログレスバーが正しい幅で表示 🔵

**Given**: `goal.is_kgi = true`, `goal.achievement_rate = 72` の `GoalWithKgi`
**When**: `KgiCard` をレンダリングする
**Then**: `role="progressbar"` 要素の `style.width` が `"72%"` である（REQ-KGI-006）

### テストケース3: `is_expired=true` で「期限超過」ラベルが表示 🔵

**Given**: `goal.is_kgi = true`, `goal.is_expired = true` の `GoalWithKgi`
**When**: `KgiCard` をレンダリングする
**Then**: テキスト「期限超過」を持つ要素が画面に表示される（EDGE-KPI-005）

### テストケース4: `days_remaining=5` で「残り 5 日」テキストが表示 🔵

**Given**: `goal.is_kgi = true`, `goal.days_remaining = 5` の `GoalWithKgi`
**When**: `KgiCard` をレンダリングする
**Then**: テキスト「残り 5 日」が画面に表示される（REQ-KGI-007）

### テストケース5: `days_remaining=-3` で「3 日超過」テキストが表示 🔵

**Given**: `goal.is_kgi = true`, `goal.days_remaining = -3` の `GoalWithKgi`
**When**: `KgiCard` をレンダリングする
**Then**: テキスト「3 日超過」が画面に表示される（EDGE-KPI-005）

### テストケース6: 「現在値を更新」ボタンをクリックするとダイアログが開く 🟡

**Given**: `goal.is_kgi = true`, `goal.metric_type = 'numeric'` の `GoalWithKgi`
**When**: 「現在値を更新」ボタンをクリックする
**Then**: `role="dialog"` を持つ要素が画面に表示される

### テストケース7: 数値入力後「保存」をクリックすると `onCurrentValueUpdate` が呼ばれる 🟡

**Given**: ダイアログが開いており、数値入力フィールドに `42` を入力した状態
**When**: 「保存」ボタンをクリックする
**Then**: `onCurrentValueUpdate(goal.id, 42)` が1回呼ばれる

---

テストファイル例 (`frontend-v2/src/components/KgiCard.test.tsx`):

```typescript
import { render, screen, fireEvent } from '@testing-library/react';
import { KgiCard } from './KgiCard';
import { GoalWithKgi } from '../types';

const baseGoal: GoalWithKgi = {
  id: 'goal-1',
  user_id: 'user-1',
  title: 'テスト目標',
  is_kgi: false,
  created_at: '2026-01-01T00:00:00Z',
  updated_at: '2026-01-01T00:00:00Z',
};

const kgiGoal: GoalWithKgi = {
  ...baseGoal,
  is_kgi: true,
  metric_type: 'numeric',
  target_value: 100,
  current_value: 72,
  unit: 'km',
  target_date: '2026-12-31',
  achievement_rate: 72,
  days_remaining: 260,
  is_expired: false,
};

describe('KgiCard', () => {
  // TC1: 通常 Goal にプログレスバーなし
  test('is_kgi=false のとき progressbar が表示されない', () => {
    render(<KgiCard goal={baseGoal} />);
    expect(screen.queryByRole('progressbar')).toBeNull();
  });

  // TC2: achievement_rate=72 → width: 72%
  test('achievement_rate=72 のとき progressbar の width が 72%', () => {
    render(<KgiCard goal={kgiGoal} />);
    const bar = screen.getByRole('progressbar');
    expect(bar).toHaveStyle({ width: '72%' });
  });

  // TC3: 期限超過バッジ
  test('is_expired=true のとき「期限超過」ラベルが表示される', () => {
    const expiredGoal = { ...kgiGoal, is_expired: true, days_remaining: -5 };
    render(<KgiCard goal={expiredGoal} />);
    expect(screen.getByText('期限超過')).toBeInTheDocument();
  });

  // TC4: 残り日数（正）
  test('days_remaining=5 のとき「残り 5 日」が表示される', () => {
    const goal = { ...kgiGoal, days_remaining: 5 };
    render(<KgiCard goal={goal} />);
    expect(screen.getByText('残り 5 日')).toBeInTheDocument();
  });

  // TC5: 超過日数（負）
  test('days_remaining=-3 のとき「3 日超過」が表示される', () => {
    const goal = { ...kgiGoal, days_remaining: -3, is_expired: true };
    render(<KgiCard goal={goal} />);
    expect(screen.getByText('3 日超過')).toBeInTheDocument();
  });

  // TC6: ダイアログが開く
  test('「現在値を更新」ボタンをクリックするとダイアログが開く', () => {
    render(<KgiCard goal={kgiGoal} />);
    fireEvent.click(screen.getByText('現在値を更新'));
    expect(screen.getByRole('dialog')).toBeInTheDocument();
  });

  // TC7: コールバック呼び出し
  test('ダイアログで値を入力して保存するとコールバックが呼ばれる', () => {
    const onUpdate = vi.fn();
    render(<KgiCard goal={kgiGoal} onCurrentValueUpdate={onUpdate} />);
    fireEvent.click(screen.getByText('現在値を更新'));
    const input = screen.getByLabelText('現在値');
    fireEvent.change(input, { target: { value: '42' } });
    fireEvent.click(screen.getByText('保存'));
    expect(onUpdate).toHaveBeenCalledTimes(1);
    expect(onUpdate).toHaveBeenCalledWith('goal-1', 42);
  });
});
```

---

## 実装手順（TDD）

1. **要件整理** `/tsumiki:tdd-requirements TASK-0035`
   - KgiCard の表示バリエーション（通常 Goal / KGI / KGI+期限超過）を列挙する
   - metric_type ごとの表示ルールを整理する

2. **テストケース洗い出し** `/tsumiki:tdd-testcases`
   - 上記「単体テスト要件」の7ケースをテストファイルに記述する
   - `@testing-library/react` を使用する

3. **Red フェーズ** `/tsumiki:tdd-red`
   - テストを実行し、全件失敗（RED）することを確認する
   - `npm run test` または `npx vitest run src/components/KgiCard.test.tsx`

4. **Green フェーズ** `/tsumiki:tdd-green`
   - `KgiCard.tsx` を実装して全テストを通過させる
   - 最小限の実装から始め、テストを1件ずつ通過させる

5. **Refactor フェーズ** `/tsumiki:tdd-refactor`
   - `formatValue`、`getDaysRemainingLabel` を独立関数として整理する
   - `UpdateDialog` をサブコンポーネントとして分離する
   - アクセシビリティ属性（`role`, `aria-*`）を確認する

6. **完了確認** `/tsumiki:tdd-verify-complete`
   - 全7テストケースが GREEN であることを確認する
   - `npm run build` でコンパイルエラーなしを確認する

---

## 信頼性レベルサマリー

- 🔵 **青信号**: 5項目 (71%)
- 🟡 **黄信号**: 2項目 (29%)

**品質評価**: 高品質（主要な表示ロジックは設計文書に基づく。ダイアログ操作はインタラクション依存のため黄信号）

## 注意事項

- `is_kgi=false` のときは既存 Goal カードの表示と同じ見た目にする（スタイルクラス名は既存コンポーネントを参照すること）
- プログレスバーの width は `Math.min(achievementRate, 100)` でキャップする（100%超過を防ぐ）
- binary 型のときは入力ダイアログを数値入力フォームではなくボタン選択式にする
- `onCurrentValueUpdate` は optional なので、ボタンは常に表示するが、コールバックが未指定でも壊れないようにする
